name: Deploy Exam Bot

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        pip install python-telegram-bot
        
    - name: List files
      run: |
        ls -la
        echo "📁 الملفات الموجودة في الـ repository:"
        
    - name: Test if bot file exists
      run: |
        if [ -f "professional_bot.py" ]; then
          python -m py_compile professional_bot.py
          echo "✅ الكود صحيح النحو"
        else
          echo "⚠️ ملف professional_bot.py مو موجود"
          echo "📝 تأكد من رفع الملف للـ repository"
        fi
